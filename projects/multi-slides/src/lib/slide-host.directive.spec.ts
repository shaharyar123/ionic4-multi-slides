import { SlideHostDirective } from './slide-host.directive';

describe('SlideHostDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideHostDirective();
    expect(directive).toBeTruthy();
  });
});
